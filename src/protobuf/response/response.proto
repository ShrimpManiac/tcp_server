// 새로운 proto 파일 추가시 packetNames.js에도 추가되어야 합니다!

syntax = "proto3";

package response;

message Response {
  uint32 handlerId = 1;
  uint32 responseCode = 2;
  int64 timestamp = 3;
  bytes data = 4;
  uint32 sequence = 5;
};