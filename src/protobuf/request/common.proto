// 새로운 proto 파일 추가시 packetNames.js에도 추가되어야 합니다!

syntax = "proto3";

package common;

message Packet {
  uint32 handlerId = 1;
  string userId = 2;
  string clientVersion = 3;
  uint32 sequence = 4;
  bytes payload = 5;
};